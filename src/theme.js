import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#f1b503',
    },
    secondary: {
      main: '#42bcfb',
    },
    tertiary: {
      main: '#3F30DA',
    },
    dark: {
      main: '#0E0E2C',
    },
    success: {
      main: '#ed4b9e',
    },
  },
});

export default theme;
